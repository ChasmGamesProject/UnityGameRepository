using UnityEngine;
using System.Collections;

public class StopRotate : MonoBehaviour {
	Quaternion StaticRotate;
	// Use this for initialization
	void Start () {
		
	//StaticRotate = transform.localRotation;
	}
	
	// Update is called once per frame
	void Update () {
	//transform.localRotation = StaticRotate;
	}
}
