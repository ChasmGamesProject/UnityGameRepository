using UnityEngine;
using System.Collections;

public class SaveManager
{
	// data itll get for creating saves;
		// Character Data; Current Mood Value & Currently Avaliable Topics
		// Current Act(?)
			// Assuming act determines NPC positions
		// Player Location (Room id & Vector3)
		// Inventory Slots
			// Note; need to delete collected objects from world...
		// Door Status (Locked/Unlocked)
	
	public void CreateSave()
	{
	}
	
	public void LoadSave()
	{
		// no input/actions, until its loaded (set interactmode to none or something)
	}
}
